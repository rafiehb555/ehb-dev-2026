import type { ReactNode } from "react";

export const metadata = {
  title: "GoSellr - AI-Powered Global Marketplace | EHB",
  description: "Buy and sell verified products globally with AI recommendations and blockchain trust verification.",
};

export default function GoSellrLayout({ children }: { children: ReactNode }) {
  return children;
}
