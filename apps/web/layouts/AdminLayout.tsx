import type { ReactNode } from "react";

/**
 * EHB AdminLayout – Super Admin sidebar & content area.
 */
export function AdminLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
