# Industry content folders (32)

Each subfolder matches `slug` in `lib/industry/config.ts`.

- **`law/ols-law-source/`** — copy of **EHB OLS LAW SERVICES** (local prototype: Next.js app, SQLite `ehb_law.db`, server). Run `npm install` inside `ols-law-source` or `ols-law-source/nextjs-app` as needed; do not commit secrets (`.env.local` was not copied).

- **Other slugs** — placeholders for future per-industry assets, imports, or docs. Marketing routes: `/landing/<slug>`, `/industry/<slug>`.

Visual assets for the main app live under `public/images/industries/` (see `public/images/industries/README.txt`).
