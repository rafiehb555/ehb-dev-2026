# Law — imported OLS source

`ols-law-source/` is a snapshot from `D:\EHB\EHB OLS LAW SERVICES` (excluding `node_modules`, `.env.local`, and `nextjs-app/.next`).

Contents include:

- **`nextjs-app/`** — standalone Next.js UI (run `npm install` then `npm run dev` inside that folder).
- **`ehb_law.db`** — SQLite data from the prototype.
- **`server.ts`**, **`package.json`**, **`metadata.json`**, docs (`DEMO_*.md`, etc.).

Integrating into the main EHB landing app is a separate step; this tree is kept as **reference + data**.
