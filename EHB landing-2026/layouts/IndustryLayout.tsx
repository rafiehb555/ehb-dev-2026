import type { ReactNode } from "react";

/**
 * EHB IndustryLayout – industry template (Hero, Search, Categories, etc.).
 */
export function IndustryLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
