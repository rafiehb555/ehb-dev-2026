import type { ReactNode } from "react";

/**
 * EHB DashboardLayout – user dashboard (profile, bookings, wallet, affiliate).
 */
export function DashboardLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
